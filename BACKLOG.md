# BACKLOG — 다음 고도화 세션에서 바로 이어서 할 것

> "알아서 이어서 해줘" 요청이 오면 이 문서 위에서부터 순서대로 진행한다.
> 우선순위 기준: Critical bug → Mobile UX → 사용성 → Core Workflow → AI Action → Customer Portal → Evidence/KPI → Visual polish → Future

---

## 이번 세션(2026-09-11)에 완료한 것

| 항목 | 상태 |
|---|---|
| A. UI/UX 복잡도 진단 | 완료 (§아래 진단 요약) |
| B. Sidebar / IA 정리 | 완료 — 4 그룹, 결과자료를 자료관리 탭으로 흡수, 승인·매출기회 신설, 그룹별 단일 색 계열 |
| C. Theme 뿌연 문제 | 완료 — soft 토큰 전면 중성화, Burgundy/Plum accent 채도 상향, Shell 텍스트를 반투명 흰색으로 |
| D. Mobile 가로스크롤 | 완료 — Kanban·자료관리·기업고객·상담/계약·기업카드(계약·자료) 전부 모바일 카드형으로 |
| E. Motion / interaction | 부분 — 기존 토큰(140~220ms) 유지, 신규 화면에 동일 적용. 상태 전이 모션은 미착수 |
| F. 대표 Dashboard | 완료 — 승인대기·매출기회를 최상단으로, 중복 KPI 제거 |
| G. 대표 승인 Workflow | 완료 — 제안·할인·고객약속 3종, 승인 시 후속 Task 자동 생성 |
| H. 고객 Portal 고도화 | 부분 — 함께 검토(서비스) 신설, 홈에 요청 현황 노출. 예상 완료시점 안내는 미착수 |
| I. 추가서비스 → Opportunity Flow | 완료 — Portal 관심표시 → 기회 생성 → 상담 Task → 대표 승인 → 제안 → 추가계약 |
| J. AI Action 구조 | 부분 — 규칙 발견(rule) 소스가 기회로 들어옴. 브리핑의 "추천 → 즉시 실행" 버튼은 미착수 |
| K. 2단계→3단계 설문 | 완료 — 9개 섹션 20문항, 전부 클릭형, 자유입력 1개, Supabase 이관 가능한 구조 |
| L. Report / Evidence | 완료 — 4축(운영 사용량·업무 효율·고객·매출) + Baseline 미수집 명시 |

### 진단 요약 (A)
1. **사이드바에 승인·매출 흐름이 없었다.** 대표의 실제 1순위 행동(승인)이 시스템에 없어서, 할인·제안은 여전히 전화로 처리되고 기록이 남지 않았다.
2. **고객 관심을 받을 곳이 없었다.** Portal은 "진행조회"에서 끝났고, 고객이 다음 서비스에 관심을 보여도 담을 그릇이 없었다.
3. **모바일이 데스크톱 축소판이었다.** Kanban 8컬럼과 880~900px 표 5개가 가로 스크롤을 강제했다.
4. **테마 색이 Surface까지 번졌다.** `--theme-soft`가 채팅 말풍선·메모·선택 행 같은 넓은 면을 칠하고 있었고, Burgundy/Plum은 accent 채도가 30%로 10종 중 최저라 탁하게 보였다.
5. **리포트가 통계였지 실증이 아니었다.** 축이 3개뿐이고 매출 축이 없어, 외부에 "AX가 사업방식을 바꿨다"를 설명할 수 없었다.

---

## 고도화 2차(2026-09-11)에 완료한 것

| 항목 | 상태 |
|---|---|
| J. AI Action 구조 | **완료** — 브리핑 10종 항목 전부에 실행 버튼. 초안·업무 등록·일정 등록·단계 변경·기회 등록·완료 처리가 그 자리에서 실행되고, 전부 기존 store action을 거쳐 Evidence로 남는다 |
| 이탈 위험 · 재상담 규칙 | **완료** — 접촉 21일(35일 긴급) / 완료 후 30일 + 진행 건 없음. 기회 등록은 자동이 아니라 담당자가 버튼으로 확정한다 |
| 상담 기록 직접 작성 | **완료** — 구조화 요약 5항목 + 다음 Action → 후속 업무 자동 등록 |

**자동 등록을 하지 않은 이유** — 규칙이 이탈 위험·재상담을 발견해도 기회를 자동 생성하지 않는다. 사람이 보기에 명백히 아닌 건이 파이프라인에 쌓이면 매출기회 숫자 자체를 못 믿게 된다. 발견은 규칙이, 확정은 담당자가 한다.

## 고도화 3차(2026-09-11)에 완료한 것

| 항목 | 상태 |
|---|---|
| 견적서 작성 | **완료** — 항목별 금액, 할인, 유효기간. 할인이 있으면 `approvalId`가 붙기 전까지 발송 버튼이 열리지 않는다 |
| 견적 → 고객 회신 | **완료** — Portal에 발송된 견적만 노출, 수락/보류 회신 → 내부 알림 + 후속 Task |
| 견적 → 계약 전환 | **완료** — 계약 생성 + 연결된 매출기회 자동 종료(won) |
| 견적 회신 대기 규칙 | **완료** — 발송 3일 무회신 확인, 7일 또는 유효기간 초과 시 긴급 |
| 브리핑 완료 되돌리기 | **완료** — 처리한 항목을 목록에 잠시 붙잡아 두고 되돌리기 제공 |
| 리포트 매출 축 | **완료** — 견적 발송 / 수락 / 회신 소요일 / 계약 전환 4지표 추가 |

할인 승인은 계약이 아니라 **견적**에 붙였다. 실제로 할인을 정하는 시점이 견적이고, 계약서는 그 결과를 옮겨 적는 문서다. 계약 화면의 할인 요청 버튼은 제거했다.

## 고도화 4차(2026-09-11)에 완료한 것

| 항목 | 상태 |
|---|---|
| 메뉴 구조 재단순화 | **완료** — 13 → 9개, 3그룹(오늘 / 고객·프로젝트 / 운영) + 하단 Utility. Why AX·설정·AX 코치는 한 단계 작게 |
| 문의 → 업무함 통합 | **완료** — `/ax/tasks` 탭. `/ax/inquiries` 라우트는 링크용으로 유지 |
| 대시보드 밀도 조절 | **완료** — KPI 6 → 4(전부 "지금 문제가 있는 것"), 브리핑 파생 중복 카드 2개 제거, 코치가 최상단 |
| 모바일 세로 길이 | **완료** — 브리핑 5942 → 4653px, 대시보드 4285 → 3464px. 긴 목록은 MoreButton으로 접음 |
| AX Coach | **완료** — 14일 스프린트, 오늘의 미션, 코치 한 줄 지시 |
| Evidence Coverage | **완료** — 6영역 실측 건수 / 목표 건수, 가장 부족한 영역 안내 |
| Report 연결 | **완료** — 리포트 첫 탭이 실증 진행 |
| 기능적 모션 | **완료** — 숫자 CountUp, 미션 stagger, 완료 pop-in |

**미션 완료 판정을 Event로 한 이유** — "버튼을 눌렀으면 완료"로 하면 미션을 위한 가짜 클릭이 가능해지고, 그 순간 실증 데이터가 근거를 잃는다. 행동의 결과가 기록됐을 때만 완료로 본다.

## 고도화 5차(2026-09-11)에 완료한 것

| 항목 | 상태 |
|---|---|
| AX 코치 시각 강조 | **완료** — 연한 brand tint 그라데이션 + accent 테두리, 3초 주기 glow(진폭 작게). 모션 끄기 설정과 `prefers-reduced-motion` 존중 |
| 왜 중요한가 부연설명 | **완료** — 자금조달·투자 심사 관점 4가지 근거. 코치 카드는 접이식, 코치/리포트 화면은 상단 고정 |
| 도입 전후 비교 | **완료** — Before 6항목 대표 직접 입력, After는 시스템 실측, 차이 자동 계산 |

**"모두 이 데이터가 있다"를 쓰지 않은 이유** — 출처를 댈 수 없는 통계는 심사자가 근거를 물으면 오히려 신뢰를 깎는다. 같은 메시지를 "조달에 성공한 기업들이 공통적으로 제시하는 것은 전후 비교 데이터"라는, 확인 가능한 형태로 바꿨다.

**Baseline을 직접 입력받는 이유** — 도입 전 값은 시스템 안에 있을 수 없다. 자동 계산하는 척하면 그 순간 전체 리포트가 가짜가 된다. 대표 입력값임을 화면에 명시하고 실측값과 분리해 표시한다.

## 고도화 6차(2026-09-11) — 자체 점검(오류 · UI/UX)에서 나온 수정

이번 회차는 새 기능이 아니라, 만들어 둔 화면을 도구로 훑어서 나온 실제 결함을 고친 것이다.

| 항목 | 상태 |
|---|---|
| Accent 대비 (10종 전수) | **완료** — 흰 배경 위 accent 글자가 2.24~3.4:1이었다. `--theme-accent-strong` 신설 |
| 상태색 대비 | **완료** — success 4.25 → 5.74, warning 3.64 → 5.57, error 4.87 → 5.94 (흰 배경 기준) |
| 보조 텍스트 대비 | **완료** — `--neutral-text-3` 3.12 → 4.79:1. 각주·단위·설명 문장이 전부 이 색이었다 |
| 14일 종료 상태 | **완료** — 15일차 이후에도 "Day 14 / 14"로 멈춰 있었다. `finished` 상태 + 리포트 유도 |
| 견적 유효기간 경과 | **완료** — 만료된 제안에 수락 버튼만 있었다. 내부·고객 양쪽에 경과일 표시(상태는 자동 변경하지 않음) |
| 모바일 터치 타깃 | **완료** — 필터 칩 31 → 40px, 업무 체크박스 실제 터치영역 28 → 44px |
| 모바일 제목 잘림 | **완료** — 일정·자료·승인 대기·매출기회 제목을 2줄까지 허용(데스크톱은 1줄 유지) |
| 렌더 중 시각 참조 | **완료** — `useAfterValues`가 렌더 중 `new Date()`를 읽어 SSR/클라이언트 값이 갈릴 수 있었다 |
| 조건부 Hook | **완료** — 견적 상세에서 early return 뒤에 Hook이 호출되고 있었다 |

**Accent를 두 값으로 나눈 이유** — accent는 원래 "면을 칠하는 색"으로 골랐는데, 실제로는 링크·숫자·CTA 라벨 같은 **글자**에 가장 많이 쓰이고 있었다. Navy Gold는 흰 배경에서 2.24:1로, 밝은 사무실 모니터에서는 사실상 안 읽힌다. 원색(`--theme-accent`)은 진행바·glow처럼 글자가 없는 면에만 남기고, 글자와 닿는 곳은 전부 `--theme-accent-strong`(4.6:1 이상)으로 보냈다. 색 계열과 테마 인상은 그대로다.

**유효기간이 지나도 상태를 자동으로 바꾸지 않는 이유** — 만료를 시스템이 `expired`로 바꿔버리면 실제로는 연장해서 진행 중인 건까지 죽은 건으로 집계된다. 사실(며칠 지났다)만 양쪽 화면에 띄우고, 연장할지 재발송할지는 사람이 정한다.

## 고도화 7차(2026-09-12) — 글자 크기 · 로고 · 테마 정리 · 모션

| 항목 | 상태 |
|---|---|
| 글자 크기 조절 | **완료** — 4단계. 지금까지의 크기(19px)가 최소값, 위로 21·23·25px |
| 조절 위치 | **완료** — AX/Portal 헤더에 `가⁻/가⁺` 스테퍼(PC·모바일 동일), 설정·내 정보·더보기에 4단계 선택 |
| 큰 글씨에서의 레이아웃 | **완료** — 사이드바·헤더 높이를 rem 기반 clamp로, 헤더의 보조 요소는 l/xl에서 접힘 |
| 사이드바 로고 | **완료** — 상단 `KPJK CORPORATION`, 아래 `Business AX`를 1.18rem으로 키움 |
| 사이드바 메뉴 위치 | **완료** — 로고 블록을 키우고 nav에 상단 여백 추가 |
| 테마 2종 제거 | **완료** — Burgundy Slate · Plum Indigo 삭제(8종). 저장된 값은 기본 테마로 자동 복구 |
| 모션 레이어 | **완료** — 라우트 전환, 사이드바 hover/활성 바, 버튼 lift, 진행바 sheen, 배지 tick, 알림 pulse, CTA 화살표, 아이콘 pop |

**글자를 더 작게는 만들지 않는 이유** — 작게 열어두면 화면에 정보를 더 담으려고 계속 줄이게 되는데, 그 방향은 이미 한 번 가독성 문제로 되돌린 적이 있다. 기준선을 지금 크기로 고정하고 위로만 연다.

**큰 글씨에서 헤더를 접는 이유** — Tailwind의 lg/xl 분기는 뷰포트 기준이라 글자를 키워도 그대로다. 그래서 23px 이상에서는 헤더의 "있으면 좋은" 것(시계·DEMO 배지·이름·시연 버튼)을 먼저 접고 알림·계정·로그아웃만 남긴다.

## 고도화 8차(2026-09-12) — 기성고 2단계 요건 대응 (②③④⑤)

기성고 평가에서 "2단계 미충족"의 직접 원인이었던 항목을 채웠다. ①(서버 DB)은 다음 회차.

| 항목 | 상태 |
|---|---|
| 기업고객 등록 · 수정 | **완료** — `createCompany` / `updateCompany`. 필수값·중복명·사업자번호·이메일 검증, 변경 필드가 활동 로그에 남음 |
| 프로젝트 등록 · 수정 | **완료** — `createProject` / `updateProject`. 단계 변경은 기존 전용 액션으로 위임(고객 Portal 진행률·알림 유지) |
| 로그인 / 인증 | **완료(클라이언트 한정)** — 아이디+비밀번호(SHA-256 해시) 검증, 실패 사유 구분, 5회 실패 시 60초 잠금, 로그인·실패·로그아웃 전부 Event 기록 |
| 역할 전환 버튼 제거 | **완료** — 헤더·더보기·설정의 대표/직원 토글 3곳 삭제. 역할은 계정으로만 결정 |
| 권한 강제 | **완료** — `src/lib/permissions.ts` 단일 정책. 쓰기 액션 25곳이 `deny()`를 통과해야 실행되고, 거절도 `permission_denied` Event로 남는다 |
| 권한표 | **완료** — 손으로 적은 12행 표를 삭제하고 정책에서 18행을 생성. 표와 실제 동작이 갈라질 수 없다 |
| 고객 Portal 미리보기 | **완료** — 읽기 전용. 내부 계정이 자료 제출·문의·회신·관심표시를 대신 할 수 없다 |
| 일정 수정 · 삭제 | **완료** — 공개 일정의 시간 변경·삭제 시 고객 알림 자동 전송 |
| 업무 수정 · 삭제 | **완료** — 제목·유형·기한·우선순위·담당자·상태·메모. 자동 생성 업무임을 모달에 표시 |

**미리보기를 읽기 전용으로 바꾼 이유** — 이전에는 내부 계정이 미리보기 중에 고객 대신 자료를 제출할 수 있었고, 그 기록이 `actorRole: client`로 남았다. 그러면 "고객이 직접 제출했다"는 실증 데이터가 거짓이 된다. 실증의 근거가 되는 Event는 실제 행위자만 만들 수 있어야 한다.

**권한 검사를 화면이 아니라 액션에 둔 이유** — 버튼을 숨기는 것만으로는 "권한이 적용된다"고 말할 수 없다. 검수자가 가장 먼저 확인하는 것이 그 지점이다. 다만 서버가 없으므로 이것은 업무 규칙이지 보안 경계가 아니며, 그 한계를 설정 화면과 코드 주석에 명시했다.

## 고도화 9차(2026-09-12) — CRUD 구멍 메우기

전체 CRUD를 훑어 빠진 것을 채웠다. 삭제가 일정·업무 2곳에만 있었고, 계정 관리 화면이 아예 없었다.

| 항목 | 상태 |
|---|---|
| 사용자 계정 관리 | **완료** — 생성·수정·사용중지/재개·비밀번호 재설정. 대표 계정만 가능 |
| 기업 상세 ↔ 계정 연결 | **완료** — 담당자 Portal 계정이 없으면 경고 + 그 자리에서 생성 |
| 자료요청 수정·취소 | **완료** — 제출 전(planned/requested/revision)에만. 고객 알림 자동 전송, 딸린 검토 업무 정리 |
| 상담기록 수정·삭제 | **완료** — 작성 폼을 그대로 재사용. 수정 시 후속 업무 재생성은 하지 않는다 |
| 기업·프로젝트 보관 | **완료** — 하드 삭제 대신. 목록·Board·브리핑·고객 Portal에서 빠지고 기록은 전부 남는다 |

**계정을 삭제하지 않고 사용 중지하는 이유** — 지우면 그 사람이 남긴 상담·검토·승인 기록의 작성자를 알 수 없게 된다. 중지된 계정은 로그인만 막히고 과거 기록과 담당 배정은 그대로다. 본인 계정과 마지막 대표 계정은 중지할 수 없다.

**기업·프로젝트를 지우지 않고 보관하는 이유** — 지우면 그 아래 자료·상담·계약·활동로그가 전부 고아가 되고 실증 데이터의 근거도 함께 사라진다. 기업을 보관하면 하위 프로젝트도 같이 보관되고, 보관 해제도 언제든 가능하다.

**자료요청을 제출 후에는 못 고치게 한 이유** — 요청 내용과 고객이 실제로 낸 자료가 어긋나면 그 기록 전체가 의미를 잃는다.

### 남은 CRUD 공백 (의도적으로 미룸)
- ~~견적 수정, 계약 직접 등록, 결과자료 회수~~ — 10차에서 완료
- 문의 삭제 — 실증 기록을 지우는 일이라 하지 않는다. 종료 처리로 충분
- 사용자 역할 변경 — 계정을 새로 만드는 쪽이 기록상 명확해 막아둠

## 고도화 10차(2026-09-15) — 4시간 자율 고도화

| 항목 | 상태 |
|---|---|
| **전역 검색** (⌘K / Ctrl+K) | **완료** — 기업·프로젝트·자료요청·업무·일정·상담·견적·계약·문의 9종을 한 입력창에서. 종류별 상위 5개, ↑↓ Enter Esc. 보관 항목·완료 업무는 제외. PC 헤더 검색창 + 모바일 아이콘 |
| 견적 수정 (발송 전) | **완료** — 작성 폼 재사용. 할인율이 바뀌면 기존 승인은 자동 무효(승인은 특정 할인율에 대한 것) |
| 계약 직접 등록·수정 | **완료** — 견적 없이 맺은 기존 계약 이관용. 종료일·금액·상태·서명일. 행 클릭으로 수정 |
| 결과자료 회수 | **완료** — 고객 화면에서 내리고 사유와 함께 안내. 회수 사실은 기록에 남음 |
| 자료화면(/ax/documents)에서 자료요청 수정 | **완료** |
| **자동 업무 규칙 5종** | **완료** — 계약 만료 D-30 갱신 협의 / 사후관리 90일 종료 점검 / 결과자료 7일 미열람 / 견적 유효기간 D-3 / 자료 기한 3일 초과 독촉. 앱 열 때 + 10분마다, `ruleKey`로 멱등 |
| 규칙 켜기/끄기 화면 | **완료** — 설정에서 대표가 관리. 규칙이 만든 업무는 업무함에 "규칙" 배지 |
| **설문 응답 집계** | **완료** — 문항별 상위 응답 막대, 역할 필터, 자유 의견 목록. 대표·컨설턴트만 |

**시간 규칙을 브리핑이 아니라 업무함에 넣은 이유** — 브리핑은 "지금 보이는 것"이고 업무함은 "처리했는지 기록되는 것"이다. 계약 만료처럼 놓치면 돌이킬 수 없는 일은 처리 여부가 남아야 한다. 같은 대상·같은 규칙으로 두 번 만들지 않도록 `ruleKey`를 둔다.

**검색 순위를 단순하게 둔 이유** — 이름이 검색어로 시작하면 3, 포함하면 2, 부가 필드에 있으면 1. 똑똑한 순위보다 "왜 이게 위에 있는지" 사용자가 바로 이해하는 순위가 낫다.

## 고도화 11차(2026-09-15) — 4시간 자율 고도화 2

기준: 서버 없이 실제 데이터를 넣기 시작해도 안전한가.

| 항목 | 상태 |
|---|---|
| **운영 모드** | **완료** — 켜면 20시간 자동 초기화 중지, 데모 초기화 잠금, 로그인 화면 데모 계정 안내 숨김, DEMO 배지 → 운영. 전환은 기록에 남음 |
| **백업 내보내기 / 가져오기** | **완료** — 전체 데이터를 JSON 한 파일로. 가져오기는 모양 검증(형식·16개 목록·사용 가능한 대표 계정) 후 교체, 실패 시 아무것도 바꾸지 않음. 사이드바에 7일 이상 미백업 경고 |
| 인쇄물 회사 정보 | **완료** — 설정에서 입력. 비우면 회사명만 나가고 나머지는 빈칸 |
| **견적서 인쇄** | **완료** — `/print/quote/[id]`. A4, 항목·할인·공급가·VAT·합계·비고·서명란. 발송 전이면 "초안" 워터마크 |
| **실증 리포트 인쇄** | **완료** — `/print/evidence`. 운영 규모, 6영역 Coverage, 도입 전후(기준선 없으면 그 절을 비움), 기록 발췌. 개선율·효과 추정 없음 |
| **모바일 빠른 승인** | **완료** — 대표 계정 모바일에 하단 승인 바 → 한 건씩 승인/반려 + 사유. 오래된 것부터 |
| **Portal 다음 예정** | **완료** — 프로젝트 수정에서 담당자가 적은 "다음 예정"만 고객에게 표시. 바뀌면 고객 알림. 시스템이 예상일을 계산하지 않음 |

**인쇄에 외부 PDF 라이브러리를 쓰지 않은 이유** — 브라우저 인쇄(⌘P → PDF 저장)로 충분하고, 의존성 하나가 늘면 유지비가 는다. `@media print`로 도구줄을 숨기고 시트 단위로 페이지를 끊는다.

**실증 리포트에서 기준선이 없으면 그 절을 비우는 이유** — "달라졌다"는 기준선 없이는 말할 수 없다. 빈 절이 심사자에게는 채워진 숫자보다 낫다.

**예상 완료일을 시스템이 계산하지 않는 이유** — 프로젝트 유형별 표준 소요일이 아직 없다. 없는 데이터로 계산하면 그 순간 예상일이 거짓이 된다. 담당자가 적은 날짜만 보여주고, 그 사실을 고객 화면에 표기한다.

## 고도화 12차(2026-09-15) — 기업고객 입력 고도화 (타자 대신 클릭 · 서류 자동 채우기 · 샘플 지우기/다시 보기)

기준: 실제 기업고객을 넣기 시작하는 대표·컨설턴트가 휴대폰에서도 1분 안에 등록을 끝낼 수 있는가.

| 항목 | 상태 |
|---|---|
| **서류로 채우기** | **완료** — 등록·수정 화면 맨 위. 사업자등록증·법인등기부등본 PDF/사진을 올리거나 글자를 붙여넣으면 기업명·사업자번호·법인등록번호·대표자·대표자 생년월일·설립일·주소(→지역 자동)·업태·종목·자본금을 읽는다. 텍스트 PDF는 `pdfjs-dist`(워커 번들 포함), 사진·스캔본은 `tesseract.js` 한국어 OCR(언어 데이터는 처음 한 번 CDN에서). 빈 폼이면 바로 채우고, 이미 값이 있으면 항목별 체크 후 덮어쓴다. 채운 항목에는 "서류" 표시 |
| **클릭 입력** | **완료** — 사업자 형태 · 업종 · 업태 · 지역(17 시도) · 임직원 규모 7구간 · 매출 규모 6구간 · 직책 · 관심 컨설팅 분야 17종(kpjkcorporation.com 자문 분야) · 유입 경로 · 담당 컨설턴트를 칩으로 고른다. 정확한 값은 언제든 직접 입력. 사업자번호·법인번호·전화는 자동 서식. "대표와 동일" 버튼. 필수는 기업명·대표자 둘뿐(담당자 비우면 대표) |
| **기업 정보 확장** | **완료** — `Company`에 선택 필드 16개 추가(entityType, corpNo, establishedAt, bizCategory, bizItem, ceoBirth, capital, region, employeeBand, revenueBand, companyPhone, website, interests, leadSource, docs, sample). 상세 헤더에 업력·업태/종목·관심 분야·서류 확인 이력 표시. 목록 카드에 관심 분야 배지. 검색에 법인번호·지역·주소 |
| **샘플 6개 지우기 / 다시 보기** | **완료** — 시드 기업에 `sample` 표식. 목록 상단 배너·설정 카드에서 한 번에 지우면 그 기업의 프로젝트·상담·계약·자료·일정·업무·문의·결과·기회·견적·승인·알림·기록·Portal 계정까지 사라지고, 직접 넣은 기업은 한 건도 건드리지 않는다. 지우면 운영 모드가 함께 켜진다(20시간 초기화가 실데이터를 지우지 않도록). "샘플 6개 보기"로 그대로 복원(코드 충돌 시 빈 글자 배정). 대표 계정만 |
| 참고 저장소 | AX-MVP-Factory-OS의 기업 정보 항목(법인번호·대표자 생년월일·설립일·업태·종목·대표번호·홈페이지)과 서류 파서 구조만 참고. 디자인·화면 구조는 가져오지 않음 |

**서류에서 읽은 값을 "확인 후" 넣는 이유** — OCR은 숫자를 틀린다(테스트 사진에서 사업자번호를 못 읽음). 잘못 채운 사업자번호는 빈칸보다 나쁘다. 그래서 확신 없는 항목은 비우고, 기존 값과 다르면 체크로 묻고, 어떤 서류를 언제 어떤 방법으로 읽었는지를 기록에 남긴다. 파일 자체는 저장하지 않는다(localStorage 용량, 개인정보). 주민등록번호 뒷자리는 읽지 않는다.

**샘플을 "보관"이 아니라 "삭제"하는 이유** — 샘플은 실제 기록이 아니다. 보관하면 보관 목록·기록·Portal 계정에 계속 남아 실데이터와 섞인다. 대신 사용자가 만든 것은 `sample` 표식이 없으므로 절대 지워지지 않고, 시드에서 언제든 다시 만들 수 있다.

**OCR·PDF 라이브러리를 이제 추가한 이유** — 11차까지는 의존성을 늘리지 않았다. 이번에는 "파일 올리면 알아서"가 요구사항이고, 그 요구를 라이브러리 없이 정직하게 채울 방법이 없다. 둘 다 동적 import라 등록 화면을 여는 것만으로는 번들이 늘지 않는다.

## 고도화 13차(2026-09-16) — 서버 연결 (Supabase)

기준: 데이터가 브라우저 한 대를 벗어나, 대표·컨설턴트·고객이 각자의 기기에서 같은 것을 보는가.
그리고 권한이 "업무 규칙"이 아니라 **서버가 강제하는 경계**가 되는가.

| 항목 | 상태 |
|---|---|
| **스키마** | **완료** — 표 19개(`supabase/setup.sql` 1부). 앱이 만드는 문자열 ID(co_xxx)를 기본키로 그대로 쓰고, 사람(profiles)만 Supabase Auth 의 UUID. 동시에 덧붙는 것(문의 메시지·제출 파일)은 별도 표, 한 사람이 통째로 저장하는 중첩 구조는 JSONB |
| **접근 권한(RLS)** | **완료** — `setup.sql` 2부. 모든 정책이 `kpjk_can_see_company()` 하나로 모인다. 고객은 자기 회사만, 그중 공개된 것만. 쓰기는 자료 제출·문의·견적 수락으로 좁힘. 기록은 추가만(수정·삭제 정책 없음 + GRANT 회수) |
| **권한 검증** | **완료** — `test/010_rls_test.sql` 32항목, 로컬 Postgres 16 에서 **전부 통과**. 고객이 남의 회사 데이터·초안 견적·상담기록에 접근 불가, 견적을 계약전환으로 못 바꿈, 스스로 대표 권한 못 가짐, 중지 계정·비로그인 차단까지 실제로 확인 |
| **로그인** | **완료** — Supabase Auth 로 교체. 비밀번호는 서버가 갖고 앱은 해시조차 만들지 않는다. 새로고침해도 세션 유지, 비밀번호 재설정 메일, 5회 실패 잠금 유지 |
| **계정 관리** | **완료** — 세션을 저장하지 않는 두 번째 연결로 `signUp` 해서 대표가 로그아웃되지 않게 계정을 만든다. service_role 키도, 별도 서버 함수도 쓰지 않는다. 비밀번호 재설정은 "대표가 정해서 알려주기" → "본인에게 재설정 메일" 로 바뀜 |
| **동기화** | **완료** — 스토어의 `set()` 을 한 번 감싸 바뀐 배열만 비교해 달라진 행을 보낸다. **쓰기 액션 45개와 화면 코드는 한 줄도 바뀌지 않았다.** 부모→자식 순서로 보내고 삭제는 역순, 한 줄(queue)에 세워 외래키 순서를 보장 |
| **파일 업로드** | **완료** — `setup.sql` 3부, 버킷 2개(제출자료/결과자료). 경로 첫 칸이 기업 ID 이고 권한 판단이 거기서 나온다. 고객 제출 → 담당자 열기, 담당자 공유 → 고객 내려받기. 링크는 60초짜리 임시 주소 |
| **컨설턴트 열람 범위** | **완료** — 설정에서 전체/내 담당 전환. `app_settings.consultant_scope` 한 줄이 RLS 전체를 바꾼다. 정책도 코드도 다시 안 쓴다 |
| **컬럼 대조 검사** | **완료** — `test/columns.test.ts`. 매퍼 16개가 내놓는 컬럼 206개가 실제 표에 있는지, 필수 컬럼을 빠뜨리지 않는지 자동 확인 |
| **설치 안내서** | **완료** — `supabase/README.md`. SQL 3개 실행 → 대표 계정 → 환경변수 → 나머지 계정 순서 |
| 실제 Supabase 연결 시험 | **미완** — 이 개발 환경은 `*.supabase.co` 로 나가는 연결이 막혀 있다. 첫 연결 확인은 대표님 PC 에서 해야 한다 |

**액션 45개를 고치지 않고 `set()` 을 감싼 이유** — 액션마다 서버 호출을 끼워 넣으면 45곳을 고쳐야 하고,
앞으로 기능을 만들 때마다 "서버 저장을 빠뜨렸는지" 매번 확인해야 한다. 한 곳에서 가로채면 그 걱정이 사라진다.
대신 마지막에 저장한 사람이 이기는 방식이 되는데, 컨설턴트 5명 규모에서는 충돌이 드물고
모든 변경이 활동 기록에 남아 추적된다. 사람이 늘면 그때 항목별 잠금을 붙이면 된다.

**데모 모드를 남겨 둔 이유** — 환경변수가 비어 있으면 앱은 예전처럼 브라우저 저장소로 돈다.
발표 중에 인터넷이 끊겨도 화면이 살아야 하고, 정책기관·고객 시연에서 실제 고객 데이터를 띄울 이유가 없다.

**서버 모드에서 막은 것** — 데모 초기화·샘플 복원·백업 덮어쓰기. 셋 다 실제 데이터를 지우거나
반쯤 복원된 상태를 만든다. 백업 *내보내기* 는 그대로 된다.

## 고도화 14차(2026-09-17) — 도입 전 기준선 조사

기준: 대표가 3~5분 안에 클릭만으로 도입 전 상태를 남길 수 있는가. 그리고 그 값이
심사자 앞에서 "이 숫자 어디서 나왔냐"는 질문을 견디는가.

| 항목 | 상태 |
|---|---|
| **설문** | **완료** — 7단계 15문항. 자유입력 1개를 뺀 14개가 전부 클릭이다. "모르겠음"을 고를 수 있고, 고르면 수치를 만들지 않고 비운다 |
| **중간 저장** | **완료** — 단계를 넘길 때마다 조용히 저장하고, 명시적 임시저장 버튼도 둔다. 닫았다 다시 와도 이어서 쓴다. 제출 후에도 수정 가능 |
| **진입점** | **완료** — 대시보드 코치 카드의 미입력 알림(끝나면 사라짐) + 리포트·AX코치의 도입 전후 비교 카드. **사이드바에 메뉴를 늘리지 않았다** — 한 번 하는 일에 상시 메뉴를 주면 9개로 줄여 둔 IA 가 다시 늘어난다 |
| **환산 규칙** | **완료** — 선택지 → 수치 변환을 `src/lib/baseline-survey.ts` 한 곳에 모으고 규칙을 주석으로 적었다. 닫힌 구간은 중앙값, 열린 구간은 보수적으로, 월 빈도는 4.33주로 나눠 주당 환산 |
| **결과 화면** | **완료** — 답변 목록이 아니라 4개 묶음(대표 반복업무 / 고객·자료 관리 / 누락·커뮤니케이션 / 운영 규모) 요약. 상단에 "대표자 직접 입력"과 날짜 명시 |
| **복사** | **완료** — 카카오톡·문서에 그대로 붙일 수 있는 글. 계산치에는 "(입력값 기준 계산)"이 붙고, 끝에 입력 기준과 측정 방식 고지가 들어간다 |
| **인쇄** | **완료** — `/print/baseline`. 전 항목을 채운 최악의 경우에도 **A4 한 장**(실제 PDF 로 확인) |
| **기존 Baseline 연결** | **완료** — 6개 지표가 조사에서 자동으로 채워진다. 두 곳에서 따로 입력하지 않도록 기존 숫자 입력 모달은 제거했다. 비교표 자체는 그대로 |
| **지표 1개 추가** | **완료** — 단순 진행상황 문의(건/주). 시스템이 `Inquiry.category = 진행상황` 으로 실측할 수 있어 비교가 성립한다 |
| **7일차 · 14일차 확장** | **구조만** — 응답에 `phase`(before/day7/day14)를 두고 시점당 한 건씩 쌓는다. 이번에는 before 만 만든다 |

**숫자를 직접 묻지 않는 이유** — 대표는 "자료요청 평균 6.2일" 같은 통계를 갖고 있지 않다.
숫자를 요구하면 지어내게 되고, 그 순간 그 값은 근거가 아니라 소설이 된다. 구간을 고르게 하고,
구간 → 수치 환산 규칙을 코드에 적어 두면 심사자가 물었을 때 답할 수 있다.

**"모르겠음"을 넣은 이유** — 억지로 채운 값 하나가 자료 전체의 신뢰를 깎는다. 비우면 화면과
인쇄물에 "미입력"으로 나가고, 복사 글에서는 아예 빠진다.

### 함께 고친 것 (기존 버그)
- **조직 공용 설정이 서버로 안 가던 문제** — 13차의 자동 동기화는 목록 15개만 비교하고
  `settings`(회사 정보·기준선·실증 시작일·시간 규칙)는 보내지 않았다. 새로고침하면 서버 값으로
  덮여 사라진다. `set()` 래퍼가 조직 공용 키를 함께 보내도록 고쳤다
- **인쇄물에 빈 페이지가 붙던 문제** — `.print-sheet` 의 `min-height: 297mm` 가 A4 와 정확히 같아
  반올림 한 픽셀에 페이지가 하나 더 생겼다. 인쇄에서는 min-height 를 풀고, 절·표 행에
  `break-inside: avoid` 를 걸어 한 장을 넘겨도 가운데서 잘리지 않게 했다

## 다음 1순위 (Core Workflow)

0. ~~서버 DB 연결~~ — **13차에서 완료.** 남은 것은 대표님 PC 에서의 첫 연결 확인(이 개발 환경은 Supabase 로 나가는 연결이 막혀 있어 여기서 못 한다)과, 실시간 반영(지금은 새로고침해야 남의 변경이 보인다).

1. ~~후속 업무 자동 생성 규칙 확장~~ — 10차에서 완료 (시간 규칙 5종)

2. **알림 채널 연동** — 규칙 켜기/끄기 화면은 10차에서 완료. 남은 것은 발송 채널(카카오/이메일).

## 2순위 (Customer Portal · 사용성)

4. ~~예상 완료 시점 안내~~ — 11차에서 완료 (담당자 입력 예정일)
5. ~~모바일 승인 처리~~ — 11차에서 완료
6. ~~전역 검색~~ — 10차에서 완료

## 3순위 (Evidence · 운영)

7. ~~Baseline 측정 모드~~ — 14차에서 Before 조사 완료. 남은 것은 7일차·14일차에 같은 문항을 다시 묻는 화면(구조는 `phase` 로 준비됨).
8. ~~설문 응답 집계 화면~~ — 10차에서 완료
9. ~~Evidence Pack PDF~~ — 11차에서 완료 (브라우저 인쇄)

## 4순위 (연결 · 인프라, CONDITIONAL)

10. Supabase Auth / RLS / Storage — 설문 응답(`SurveyResponse`)과 `Opportunity`·`Approval`은 이미 그대로 테이블로 옮길 수 있는 형태다.
11. LLM 연결 — AI-01 상담 요약, AI-05 톤 조정.
12. 카카오 알림톡 · 이메일 발송 (NEXT-01).
13. 전자계약 서명 연동 (NEXT-05).

## 의도적으로 하지 않는 것

멀티테넌트 SaaS · 범용 정책자금 추천엔진 · Marketplace · PG 결제 · 자동 마케팅 · Native App · 외부기관 실시간 API.
KPJK 실사용 6개월 + Baseline 개선 확인 후 재검토한다.

## 알려진 잔여 이슈

- Motion: 상태 전이(자료 제출 → Task 생성 → 검토 완료 → Portal 진행률 변경)를 시각적으로 이어주는 연출은 아직 없다. 각 화면의 진입 애니메이션만 있다.
- AX 코치 미션은 담당자 구분이 없다. 여러 명이 쓰면 "누가 했는지"가 아니라 "회사 전체에 기록이 있는지"로 판정된다.
- 실증 시작일은 설정 한 곳에만 있다. 계정별 스프린트는 지원하지 않는다.
- 도입 전후 비교에서 "대표가 직접 챙긴 비중"·"하루 수기 확인 시간"·"자료 찾는 시간"·"재작업"은 시스템이 셀 수 없어 After 가 비어 있다. 7일차·14일차에 같은 문항으로 다시 받아야 한다 (14차에서 구조 준비).
- 견적 유효기간이 지나도 상태는 자동으로 바뀌지 않는다(의도). 브리핑 긴급 + 내부/고객 화면의 경과일 안내로 알린다.
- Theme 8종 대비(contrast)는 자동 측정으로 전수 검증 완료. 육안 검증은 KPJK Signature · Navy Gold 2종.
- ~~파일은 메타만 저장한다~~ — 13차에서 실제 업로드 완료. 단 데모 모드(환경변수 없음)에서는 여전히 메타만 남는다.
- 실시간 반영 없음 — 다른 사람이 바꾼 내용은 새로고침해야 보인다.
- 같은 행을 동시에 고치면 마지막 저장이 이긴다.
- 알림 읽음 표시는 내부 계정끼리 공유된다(한 명이 읽으면 모두 읽음). 서버 연결과 무관한 기존 동작.
- 20시간 경과 시 Demo 자동 Reseed — 운영 모드를 켜면 멈춘다 (11차). 샘플을 지우면 운영 모드가 자동으로 켜진다 (12차)
- 사진 OCR은 인터넷이 필요하다(언어 데이터 CDN). 오프라인·차단 환경에서는 PDF 또는 붙여넣기만 된다. 이 사실은 오류 문구로 안내한다
